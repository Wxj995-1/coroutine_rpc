# Generated by coroutine_rpc coroutine_rpc_generator.py
# Create Time: ${CREATE_TIME}
# RPC 服务监听地址
rpcserverip=127.0.0.1
rpcserverport=${CONF_PORT}

# ZooKeeper 地址
zookeeperip=127.0.0.1
zookeeperport=${ZK_PORT}

# 日志输出目录（生成器写入绝对路径，固定到 <工程>/build/bin/log，与启动目录无关）
log_path=${LOG_PATH}
log_prefix=${LOG_PREFIX}
log_max_file_size=5
log_enable=true
rpc_log_level=DEBUG
app_log_level=DEBUG
log_sync_interval=500

# 框架 IO 线程数
iothread_num=${IO_THREAD_NUM}
