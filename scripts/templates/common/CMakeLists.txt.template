# Generated by coroutine_rpc coroutine_rpc_generator.py
# Create Time: ${CREATE_TIME}
cmake_minimum_required(VERSION 3.10.2)
project(${PROJECT_NAME} LANGUAGES CXX)

if(NOT CMAKE_BUILD_TYPE)
  set(CMAKE_BUILD_TYPE Debug)
endif()

# Keep all generated artifacts in the build tree.
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/lib)

# Point MPRPC_ROOT to the coroutine_rpc repository. The framework static
# library is rebuilt here from the repo sources so this example stays
# independent from the repo build tree.
set(MPRPC_ROOT "${SRC_ROOT_DEFAULT}" CACHE PATH
    "coroutine_rpc repository root (its src/ is compiled into mprpc)")
if(NOT MPRPC_ROOT OR NOT EXISTS "${MPRPC_ROOT}/src")
  message(FATAL_ERROR
    "MPRPC_ROOT is not set or invalid. Configure with: cmake -DMPRPC_ROOT=<coroutine_rpc repo>")
endif()

enable_language(ASM)

file(GLOB_RECURSE MPRPC_SOURCES CONFIGURE_DEPENDS
  "${MPRPC_ROOT}/src/*.cc"
  "${MPRPC_ROOT}/src/*.S"
)
add_library(mprpc STATIC ${MPRPC_SOURCES})
target_compile_features(mprpc PUBLIC cxx_std_14)
target_compile_definitions(mprpc PRIVATE _GNU_SOURCE)
target_include_directories(mprpc PUBLIC ${MPRPC_ROOT}/src)
target_link_libraries(mprpc PUBLIC pthread zookeeper_mt protobuf dl)

${PROTO_CMAKE}
${EXAMPLE_TARGETS}
