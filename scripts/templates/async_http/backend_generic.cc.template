/*************************************************************
 * Generated by coroutine_rpc coroutine_rpc_generator.py
 * Create Time: ${CREATE_TIME}
 * Generic async HTTP demo backend (RPC provider) skeleton. It compiles for
 * any proto3 service and implements every method with an empty body.
 *************************************************************/
#include <unistd.h>

#include "${PROTO_BASENAME}.pb.h"
#include "application/rpcapplication.h"
#include "comm/log.h"
#include "rpc/rpcprovider.h"

class ${SVC}Impl : public ${SVCFULL} {
 public:
${OVERRIDES}
};

int main(int argc, char** argv) {
  RpcApplication::Init(argc, argv);

  RpcProvider provider;
  provider.NotifyService(new ${SVC}Impl());
  provider.Run();
  return 0;
}
